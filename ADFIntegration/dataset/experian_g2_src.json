{
	"name": "experian_g2_src",
	"properties": {
		"linkedServiceName": {
			"referenceName": "Gen2_Domain_ADLS",
			"type": "LinkedServiceReference"
		},
		"parameters": {
			"run_date": {
				"type": "string"
			}
		},
		"folder": {
			"name": "CDM"
		},
		"annotations": [],
		"type": "Binary",
		"typeProperties": {
			"location": {
				"type": "AzureBlobFSLocation",
				"fileSystem": {
					"value": "@{concat('customer-cdm/prod/data', '/year=',formatDateTime(addDays(dataset().run_date,-1),'yyyy'), '/month=',formatDateTime(adddays(dataset().run_date,-1),'MM'),'/day=',formatDateTime(addDays(dataset().run_date,-1),'dd'),'/')}",
					"type": "Expression"
				}
			},
			"compression": {
				"type": "ZipDeflate",
				"level": "Fastest"
			}
		}
	}
}